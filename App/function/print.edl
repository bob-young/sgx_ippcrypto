enclave {
    
    untrusted {
	//ocall generally not add a public-attribut
        void ocall_print_string([in, string] const char *str);
	void ocall_print_buffer([in,size=100] unsigned char* str,int len);
    };
};
